{
  "name": "lambdabooks.github.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^1.0.0",
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "react-scripts": "1.0.13",
    "react-tweet-embed": "^1.0.8",
    "spectacle": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -b master -m \"Updated at $(date +'%a, %d %b %Y, %T, %Z')\" --add"
  }
}
